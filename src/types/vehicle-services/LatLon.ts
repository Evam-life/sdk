/**
 * Represents a latitude longitude pair of coordinates.
 * @property {number} latitude the latitude coordinate.
 * @property {longitude} the longitude coordinate.
 */
export type LatLon = {
  latitude: number;
  longitude: number;
};
